import { StyleTextDirective } from './style-text.directive';

describe('StyleTextDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleTextDirective();
    expect(directive).toBeTruthy();
  });
});
